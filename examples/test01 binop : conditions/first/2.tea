fn main()
{
    let a:int = 10;
    let i:int = 0;
    while(i<a)
    {
        putint(i);
        let b:int = i/2;
        if( ((i-b*2) ==1) ||( i==4 ))
        {
            putch(33);
        }
        

        putch(32);

        i=i+1;
    }
}