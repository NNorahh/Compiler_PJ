fn re(start[1]:int,end[1]:int){
    while(start[0] < end[0])
    {
        let dom:int = 0;
        start[0] = start[0]+1;
    }
}
fn main()->int {
    _sysy_starttime(2);
    let i:int = 0;
    let a:int = (1 < 9) && (i > 0);
    while(i < 1000000) {
        let dom:int = 0;
        i = i + 1;
    }
    let aa:int = 0;
    let bb:int =1000000;

    re(aa,bb);
    putint(a);
    _sysy_stoptime(10);
    ret 0;
    let b:int = 1;
    ret b;
    let c:int = 2;
    ret c;
    let d:int = 3;
    ret d;
    let e:int = 4;
    ret e;
    let f:int = 5;
}